- name: Expand the environment
  hosts: webservers
  become: true
  gather_facts: true
  vars_files:
    - ../vars/default.yml
  tasks:
    - name: Copy code to remote server
      ansible.builtin.include_tasks: ../tasks/sync_code.yml
